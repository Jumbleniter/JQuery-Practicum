{
  "name": "node-starter",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "server": "json-server --watch db.json --static ./public"
  },
  "devDependencies": {
    "json-server": "^0.17.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  }
}
